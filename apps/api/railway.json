{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm -F @pos-tercos/types build && pnpm -F @pos-tercos/domain build && pnpm -F @pos-tercos/api exec prisma generate && pnpm -F @pos-tercos/api build"
  },
  "deploy": {
    "startCommand": "pnpm -F @pos-tercos/api exec prisma migrate deploy && pnpm -F @pos-tercos/api start",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 30,
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
